// Very simple, given a number (integer / decimal / both depending on the language),
// // find its opposite (additive inverse).

Examples: function getOpesit(n) {
  if (n > 0) {
    console.log(n * -1);
  }
  if (n < 0) {
    console.log(n * -1);
  }
}
getOpesit(2);

// Way 2

function getOpposite(n) {
  console.log(n * -1);
}
getOpposite(5);
